<Page
    x:Class="SqlFroega.Views.ModuleAdminView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:SqlFroega.ViewModels"
    Background="{ThemeResource AppPageBackgroundBrush}">

    <Page.DataContext>
        <vm:ModuleAdminViewModel/>
    </Page.DataContext>

    <Grid Padding="16" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Modul Verwaltung" FontSize="22" FontWeight="SemiBold"/>
                <TextBlock Text="Main-/Related-Module zentral pflegen" Opacity="0.75"/>
            </StackPanel>

            <Button Grid.Column="1" Content="Neu" Command="{Binding NewModuleCommand}"/>
            <Button Grid.Column="2" Content="Speichern" Command="{Binding SaveCommand}"/>
            <Button Grid.Column="3" Content="Löschen" Click="DeleteButton_Click"/>
        </Grid>

        <Grid Grid.Row="1" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    CornerRadius="12"
                    BorderThickness="1"
                    BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    Padding="8">
                <ListView ItemsSource="{Binding Modules}" SelectedItem="{Binding Selected, Mode=TwoWay}"/>
            </Border>

            <Border Grid.Column="1"
                    CornerRadius="12"
                    BorderThickness="1"
                    BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    Padding="12">
                <StackPanel Spacing="8">
                    <TextBlock Text="Modulname" Opacity="0.75"/>
                    <TextBox Text="{Binding ModuleName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="Neue Module dürfen ausschließlich hier angelegt werden." Opacity="0.75" TextWrapping="WrapWholeWords"/>
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Row="2" ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ProgressRing Width="18" Height="18" IsActive="{Binding IsBusy}" />
            <TextBlock Grid.Column="1" Text="{Binding Error}" Foreground="Tomato" TextWrapping="WrapWholeWords"/>
        </Grid>
    </Grid>
</Page>
