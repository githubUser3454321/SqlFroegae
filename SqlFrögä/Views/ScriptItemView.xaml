<Page
    x:Class="SqlFroega.Views.ScriptItemView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:SqlFroega.ViewModels"
    xmlns:models="using:SqlFroega.Application.Models"
    xmlns:converters="using:SqlFroega.Converters"
    Background="{ThemeResource AppPageBackgroundBrush}">

    <Page.Resources>
        <Style x:Key="HistoryDialogCloseButtonStyle" TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
            <Setter Property="Background" Value="{ThemeResource AccentFillColorDefaultBrush}"/>
            <Setter Property="Foreground" Value="{ThemeResource TextOnAccentFillColorPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource AccentFillColorDefaultBrush}"/>
        </Style>
    </Page.Resources>

    <Page.DataContext>
        <vm:ScriptItemViewModel />
    </Page.DataContext>

    <Grid Padding="16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top Bar -->
        <Grid Grid.Row="0" ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Content="← Back" Click="Back_Click"/>

            <StackPanel Grid.Column="1">
                <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding Key}" Opacity="0.7" TextTrimming="CharacterEllipsis"/>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <Button Content="Copy" Command="{Binding CopyCommand}" />
                <Button Content="Save" Command="{Binding SaveCommand}" IsEnabled="{Binding IsReadOnlyMode, Converter={StaticResource BooleanNegationConverter}}" />
            </StackPanel>
        </Grid>

        <!-- Editor Card -->
        <Border Grid.Row="1"
                CornerRadius="12"
                BorderThickness="1"
                BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                Padding="12">

            <Grid RowSpacing="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Meta -->
                <Grid Grid.Row="0" ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="Name" Opacity="0.75"/>
                        <TextBox Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="{Binding IsReadOnlyMode}" />
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Key (Path)" Opacity="0.75"/>
                        <TextBox Text="{Binding Key, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="{Binding IsReadOnlyMode}" />
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Scope" Opacity="0.75"/>
                        <ComboBox SelectedIndex="{Binding Scope, Mode=TwoWay}" IsEnabled="{Binding IsReadOnlyMode, Converter={StaticResource BooleanNegationConverter}}">
                            <ComboBoxItem Content="Global"/>
                            <ComboBoxItem Content="Customer"/>
                            <ComboBoxItem Content="Module"/>
                        </ComboBox>
                    </StackPanel>
                </Grid>

                <Grid Grid.Row="1" ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="Module" Opacity="0.75"/>
                        <TextBox Text="{Binding Module, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="{Binding IsReadOnlyMode}" />
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Tags (comma separated)" Opacity="0.75"/>
                        <TextBox Text="{Binding TagsText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="{Binding IsReadOnlyMode}" />
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="CustomerId (optional)" Opacity="0.75"/>
                        <TextBox Text="{Binding CustomerIdText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="{Binding IsReadOnlyMode}" />
                    </StackPanel>
                </Grid>

                <!-- Content -->
                <Grid Grid.Row="2" RowSpacing="6">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="SQL Content" Opacity="0.75"/>
                    <TextBox Grid.Row="1"
                             Text="{Binding Content, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             AcceptsReturn="True"
                             TextWrapping="NoWrap"
                             FontFamily="Consolas"
                             IsReadOnly="{Binding IsReadOnlyMode}"
                             VerticalAlignment="Stretch"
                             HorizontalAlignment="Stretch"
                             MinHeight="260"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             ScrollViewer.HorizontalScrollBarVisibility="Auto"/>
                </Grid>
            </Grid>
        </Border>

        <!-- History -->
        <Border Grid.Row="2"
                CornerRadius="12"
                BorderThickness="1"
                BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                Padding="12">
            <Grid RowSpacing="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="180"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" ColumnSpacing="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="History (Temporal)" FontWeight="SemiBold"/>
                    <Button Grid.Column="1" Content="Refresh" Command="{Binding RefreshHistoryCommand}"/>
                </Grid>

                <TextBlock Grid.Row="1" Text="{Binding HistoryCountText}" Opacity="0.75"/>

                <ListView Grid.Row="2" ItemsSource="{Binding HistoryItems}" IsItemClickEnabled="True" ItemClick="HistoryItems_ItemClick">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:ScriptHistoryItem">
                            <Grid ColumnSpacing="12" Padding="4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="{Binding ValidFrom}"/>
                                <TextBlock Grid.Column="1" Text="{Binding ValidTo}"/>
                                <TextBlock Grid.Column="2" Text="{Binding ChangedBy}" TextTrimming="CharacterEllipsis"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>

        <!-- Status -->
        <Grid Grid.Row="3" ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ProgressRing Width="18" Height="18" IsActive="{Binding IsBusy}" />
            <TextBlock Grid.Column="1" Text="{Binding Error}" Foreground="Tomato" TextWrapping="WrapWholeWords"/>
        </Grid>
    </Grid>
</Page>
