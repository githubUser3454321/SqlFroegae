<Page
    x:Class="SqlFroega.Views.ScriptItemView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:SqlFroega.ViewModels"
    xmlns:models="using:SqlFroega.Application.Models"
    mc:Ignorable="d"
    Background="{ThemeResource AppPageBackgroundBrush}">

    <Page.DataContext>
        <vm:ScriptItemViewModel/>
    </Page.DataContext>

    <Grid Padding="16" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="{Binding Title}" FontSize="22" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <Button Grid.Column="1" Content="Save" Command="{Binding SaveCommand}"/>
            <AutoSuggestBox Grid.Column="2"
                            PlaceholderText="Kundenkürzel (z.B. KDN)"
                            Text="{Binding SelectedCustomerCode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            ItemsSource="{Binding CustomerMappings}"
                            DisplayMemberPath="CustomerCode"
                            TextMemberPath="CustomerCode"/>
            <Button Grid.Column="3" Content="Copy" Command="{Binding CopyCommand}" />
            <Button Grid.Column="4" Content="Copy Rendered" Command="{Binding CopyRenderedCommand}"/>
        </Grid>

        <Border Grid.Row="1"
                CornerRadius="12"
                BorderThickness="1"
                BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                Padding="12">

            <Grid RowSpacing="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" ColumnSpacing="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Name" Opacity="0.75"/>
                        <TextBox Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="{Binding IsReadOnlyMode}" />
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Key" Opacity="0.75"/>
                        <TextBox Text="{Binding Key, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="{Binding IsReadOnlyMode}" />
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Scope" Opacity="0.75"/>
                        <ComboBox SelectedIndex="{Binding Scope, Mode=TwoWay}" IsEnabled="{Binding IsReadOnlyMode, Converter={StaticResource BooleanNegationConverter}}">
                            <ComboBoxItem Content="Global"/>
                            <ComboBoxItem Content="Customer"/>
                            <ComboBoxItem Content="Module"/>
                        </ComboBox>
                    </StackPanel>
                </Grid>

                <Grid Grid.Row="1" ColumnSpacing="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Main-Modul" Opacity="0.75"/>
                        <TextBox Text="{Binding MainModule, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="{Binding IsReadOnlyMode}" />
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Related Modules (comma separated)" Opacity="0.75"/>
                        <TextBox Text="{Binding RelatedModulesText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="{Binding IsReadOnlyMode}" />
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Tags (comma separated)" Opacity="0.75"/>
                        <TextBox Text="{Binding TagsText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="{Binding IsReadOnlyMode}" />
                    </StackPanel>

                    <StackPanel Grid.Column="3">
                        <TextBlock Text="Kundenkürzel (z.B. KDN)" Opacity="0.75"/>
                        <AutoSuggestBox PlaceholderText="Kundenkürzel (z.B. KDN)"
                                        Text="{Binding ScriptCustomerCode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        ItemsSource="{Binding CustomerMappings}"
                                        DisplayMemberPath="CustomerCode"
                                        TextMemberPath="CustomerCode"
                                        IsEnabled="{Binding IsReadOnlyMode, Converter={StaticResource BooleanNegationConverter}}"/>
                    </StackPanel>
                </Grid>

                <CheckBox Grid.Row="2"
                          Content="DB-User und Prefix ersetzen (inkl. Sicherheitsprüfung auf USE/DB-Qualifier)"
                          IsChecked="{Binding ReplaceDatabaseUserAndPrefix, Mode=TwoWay}"
                          IsEnabled="{Binding IsReadOnlyMode, Converter={StaticResource BooleanNegationConverter}}"/>

                <Grid Grid.Row="3" RowSpacing="6">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="SQL Content" Opacity="0.75"/>
                    <TextBox Grid.Row="1"
                             Text="{Binding Content, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             AcceptsReturn="True"
                             TextWrapping="NoWrap"
                             FontFamily="Consolas"
                             IsReadOnly="{Binding IsReadOnlyMode}"
                             VerticalAlignment="Stretch"
                             HorizontalAlignment="Stretch"
                             MinHeight="260"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             ScrollViewer.HorizontalScrollBarVisibility="Auto"/>
                </Grid>
            </Grid>
        </Border>

        <Border Grid.Row="2"
                CornerRadius="12"
                BorderThickness="1"
                BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                Padding="12">
            <Grid RowSpacing="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="180"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" ColumnSpacing="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="History (Temporal)" FontWeight="SemiBold"/>
                    <Button Grid.Column="1" Content="Refresh" Command="{Binding RefreshHistoryCommand}"/>
                </Grid>

                <TextBlock Grid.Row="1" Text="{Binding HistoryCountText}" Opacity="0.75"/>

                <ListView Grid.Row="2" ItemsSource="{Binding HistoryItems}" IsItemClickEnabled="True" ItemClick="HistoryItems_ItemClick">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:ScriptHistoryItem">
                            <Grid ColumnSpacing="12" Padding="4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="{Binding ValidFrom}"/>
                                <TextBlock Grid.Column="1" Text="{Binding ValidTo}"/>
                                <TextBlock Grid.Column="2" Text="{Binding ChangedBy}" TextTrimming="CharacterEllipsis"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>

        <Grid Grid.Row="3" ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ProgressRing Width="18" Height="18" IsActive="{Binding IsBusy}" />
            <TextBlock Grid.Column="1" Text="{Binding Error}" Foreground="Tomato" TextWrapping="WrapWholeWords"/>
        </Grid>
    </Grid>
</Page>
