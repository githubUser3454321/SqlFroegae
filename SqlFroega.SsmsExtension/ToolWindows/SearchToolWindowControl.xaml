<UserControl x:Class="SqlFroega.SsmsExtension.ToolWindows.SearchToolWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="320"
             d:DesignWidth="420">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock FontWeight="SemiBold" Text="SqlFroega Volltextsuche" />

        <TextBox Grid.Row="1"
                 Margin="0,8,0,12"
                 Height="28"
                 VerticalContentAlignment="Center"
                 ToolTip="Suchbegriff eingeben"
                 Text="" />

        <ListView Grid.Row="2">
            <ListViewItem Content="(Platzhalter) Treffer erscheinen hier" />
        </ListView>
    </Grid>
</UserControl>
